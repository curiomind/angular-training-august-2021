import { ChangeclassDirective } from './changeclass.directive';

describe('ChangeclassDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeclassDirective();
    expect(directive).toBeTruthy();
  });
});
